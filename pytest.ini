[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    regression: deterministic scenario regression tests
    integration: end-to-end dictionary -> kobo/pages pipeline tests
    negative: failure-mode and corruption-detection tests
